let express,mongoose,cors,readdirSync,morgan;_00e‍.w("express",[["default",["express"],function(v){express=v}]]);_00e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_00e‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_00e‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_00e‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);





require("dotenv").config();

const app = express();
const http = require("http").createServer(app);
const io = require("socket.io")(http, {
  cors: {
    origin: ["http://localhost:3000"],
    methods: ["GET", "POST"],
    allowedHeaders: ["Content-Type"],
  },
});

const corsOptions = {
  origin: ["http://localhost:3000"],
  optionsSuccessStatus: 200,
};

// db connecting

mongoose
  .connect(process.env.DATABASE_URL)
  .then(() => {
    console.log("DB connected");
  })
  .catch((err) => _00e‍.g.console.error("DB CONNECTION ERROR =>", err));

app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: [process.env.CLIENT_URL],
  })
);

readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

app.get("/", (req, res) => {
  res.send("Hello, world!");
});
// socketio
io.on("connect", (socket) => {
  // console.log("SOCKET>IO", socket.id);
  socket.on("new-post", (newPost) => {
    // console.log("socketio new post => ", newPost);
    socket.broadcast.emit("new-post", newPost);
  });
});

const port = process.env.PORT || 5001;

http.listen(port, () => console.log(`Server is running on port ${port}`));
